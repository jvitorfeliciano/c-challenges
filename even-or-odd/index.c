#include <stdio.h>

int main()
{
    while (true) // {} determine the stataments to be executed, if there is no {} just one statatement will be executed
    {
        int number;

        printf("Enter the name: ");
        scanf("%d", &number);

        if (number % == 0)
        {
            printf("The number is even");
        }
        else
        {
            printf("The number is odd")
        }
    }
}